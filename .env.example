# Copy to .env and fill in. .env is gitignored -- never commit a real token.
#
# Production token, from https://dash.tradier.com/settings/api
TRADIER_ACCESS_TOKEN=

# Sandbox token (different value). Only needed if you run with --sandbox.
TRADIER_SANDBOX_TOKEN=
